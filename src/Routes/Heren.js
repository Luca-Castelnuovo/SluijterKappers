import React from 'react';
import withBaseRoute from 'Utils/withBaseRoute';

const Heren = () => {
    return (
        <section className="section">
            <div className="container">
                <h1 className="title">Heren</h1>
            </div>
        </section>
    );
};

export default withBaseRoute(Heren);
