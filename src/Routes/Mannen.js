import React from 'react';
import withBaseRoute from 'Utils/withBaseRoute';

const Mannen = () => {
    return (
        <section className="section">
            <div className="container">
                <h1 className="title">Mannen</h1>
            </div>
        </section>
    );
};

export default withBaseRoute(Mannen);
