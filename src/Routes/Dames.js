import React from 'react';
import withBaseRoute from 'Utils/withBaseRoute';

const Dames = () => {
    return (
        <section className="section">
            <div className="container">
                <h1 className="title">Dames</h1>
            </div>
        </section>
    );
};

export default withBaseRoute(Dames);
