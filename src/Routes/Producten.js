import React from 'react';
import withBaseRoute from 'Utils/withBaseRoute';

const Producten = () => {
    return (
        <section className="section">
            <div className="container">
                <h1 className="title">Producten</h1>
            </div>
        </section>
    );
};

export default withBaseRoute(Producten);
